# Local configuration (contains secrets)
config/provider-box.env
config/unbound.records
config/dns.seed

# Editor files
.vscode/
*.swp

# OS files
.DS_Store

# Temporary and backup files
*.tmp
*.bak
*.log

# Optional: local test artifacts
*.out

# Provider Box runtime data and secrets (live on the host, never in git)
/dns-sync/
/dashboard/
/netbox/
/runtime/
/step-ca/
/technitium/
/authentik/
/config/provider-box.env
/config/unbound.records
